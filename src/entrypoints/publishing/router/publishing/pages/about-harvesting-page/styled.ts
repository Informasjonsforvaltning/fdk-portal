import styled from 'styled-components';
import { theme } from '@fellesdatakatalog/theme';

const AboutHarvestingPage = styled.article`
  flex: 1;
  margin-top: ${theme.spacing('S48')};
`;

export default { AboutHarvestingPage };
