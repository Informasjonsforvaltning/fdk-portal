import styled from 'styled-components';
import { theme } from '@fellesdatakatalog/theme';

const AboutHarvestingPage = styled.div`
  margin-top: ${theme.spacing('S72')};
`;

export default { AboutHarvestingPage };
