import styled from 'styled-components';

const Root = styled.main`
  display: flex;
  flex-direction: column;
  flex: 1;
  height: 80px;
  width: 1140px;
  margin: 0 auto;
`;

export default { Root };
