export enum SearchTypes {
  dataset,
  api,
  concept,
  informationModel
}

export enum DataFormat {
  JSON = 'application/json',
  CSV = 'text/csv',
  XML = 'application/xml',
  UNKNOWN = 'unknown'
}
