import styled from 'styled-components';
import { theme } from '@fellesdatakatalog/theme';

const Description = styled.p``;

const Sources = styled.div`
  margin-top: ${theme.spacing('S12')};
`;

export default { Description, Sources };
