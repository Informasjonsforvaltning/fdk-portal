import styled from 'styled-components';

const Section = styled.section`
  margin: 3rem 0;
`;

const DatasetDistributionsTitle = styled.h2`
  margin: 0;
  margin-bottom: 16px;
`;

export default { DatasetDistributionsTitle, Section };
